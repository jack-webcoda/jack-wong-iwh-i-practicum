//- ** This is a sample of a pug template and how it uses the data passed to it from index.js.

//- doctype html
html
    head
        title= `${title}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/reset.css")
        link(rel="stylesheet", href="/css/style.css")
        link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css")
    body
        .container
            .mt-4.mb-4
                h1 Custom object - Airlines
            a.my-4.d-block(href="/update-cobj") Add/Update this table
            table.table.border
                thead
                    tr
                        th.fw-bold Airline Name
                        th.fw-bold Airline Code
                        th.fw-bold Country
                tbody
                    each airline in data
                        tr
                            td #{airline.properties.airline_name}
                            td #{airline.properties.airline_code}
                            td #{airline.properties.country}